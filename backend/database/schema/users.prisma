model User {
  id          Int          @id @default(autoincrement())
  name        String
  permissions Permission[]
}

model Permission {
  id    Int    @id @default(autoincrement())
  name  String @unique
  users User[]
}
